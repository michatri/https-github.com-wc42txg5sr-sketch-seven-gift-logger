# ESP32 — กดตัวเลขสูงสุด 6 หลัก

โปรแกรมสำหรับ **ESP32 + จอ LCD 16x2 (I2C) + คีย์แพด 4x3**

- กดตัวเลข `0-9` ได้สูงสุด **6 หลัก**
- กด `*` เพื่อลบตัวสุดท้าย
- กด `#` เพื่อยืนยัน
- ค่าที่พิมพ์แสดงบนจอบรรทัดล่าง และส่งออกทาง Serial Monitor

## สิ่งที่ต้องใช้

- บอร์ด ESP32 (เช่น DevKit V1)
- จอ LCD 1602 พร้อมโมดูล I2C (ขา 4 เส้น)
- คีย์แพดเมมเบรน 4 แถว × 3 คอลัมน์
- สายไฟ Dupont

## ไลบรารีใน Arduino IDE

ติดตั้งจาก **Sketch → Include Library → Manage Libraries**

1. `Keypad` โดย Mark Stanley, Alexander Brevig
2. `LiquidCrystal I2C` โดย Frank de Brabander

ตั้งค่าบอร์ด: **ESP32 Dev Module**, ความเร็ว Serial **115200**

## การต่อสาย

### จอ LCD 16x2 แบบ I2C

| LCD I2C | ESP32 |
|---------|-------|
| VCC     | 5V (หรือ 3.3V ตามโมดูล) |
| GND     | GND |
| SDA     | GPIO 21 |
| SCL     | GPIO 22 |

ที่อยู่ I2C ที่พบบ่อยคือ `0x27` หรือ `0x3F` โปรแกรมจะสแกนให้อัตโนมัติตอนเปิดเครื่อง

### คีย์แพด 4x3

แผงส่วนใหญ่มี 7 ขา เรียงจากซ้ายไปขวา: **R1 R2 R3 R4 C1 C2 C3**

```
  [1] [2] [3]
  [4] [5] [6]
  [7] [8] [9]
  [*] [0] [#]
```

| ขาคีย์แพด | ความหมาย | ESP32 |
|-----------|----------|-------|
| 1         | แถว 1    | GPIO 13 |
| 2         | แถว 2    | GPIO 12 |
| 3         | แถว 3    | GPIO 14 |
| 4         | แถว 4    | GPIO 27 |
| 5         | คอลัมน์ 1 | GPIO 26 |
| 6         | คอลัมน์ 2 | GPIO 25 |
| 7         | คอลัมน์ 3 | GPIO 33 |

ถ้าปุ่มขึ้นคนละตัว ให้สลับอาร์เรย์ `rowPins` / `colPins` ในไฟล์ `.ino`

### จอ LCD 16x2 แบบขนาน (ไม่มีโมดูล I2C)

ตั้งค่า `#define USE_I2C_LCD 0` ในไฟล์ `.ino` แล้วต่อสายดังนี้

| LCD | ESP32 |
|-----|-------|
| RS  | GPIO 4 |
| E   | GPIO 16 |
| D4  | GPIO 17 |
| D5  | GPIO 18 |
| D6  | GPIO 19 |
| D7  | GPIO 23 |
| VSS | GND |
| VDD | 5V |
| V0  | วอลลุ่มปรับความคม |
| RW  | GND |
| A   | 5V ผ่านตัวต้านทาน 220Ω |
| K   | GND |

## การใช้งาน

1. อัปโหลด `esp32-6digit-keypad.ino`
2. เปิด Serial Monitor ที่ 115200
3. จอจะขึ้น `Enter number` และ `______`
4. กดตัวเลขได้ไม่เกิน 6 ตัว เช่น `250823`
5. กด `#` เพื่อยืนยัน — จอขึ้น `Confirmed` ประมาณ 2 วินาที แล้วเริ่มกรอกใหม่

ข้อความใน Serial:

```
KEY: 2
DIGIT: 2  VALUE: 2
KEY: #
CONFIRM: 250823
```

## แก้ปัญหาเบื้องต้น

- **จอไม่ติด / มีแต่กล่อง**: หมุนวอลลุ่มด้านหลังโมดูล I2C และดูผล `Scanning I2C...` ใน Serial
- **ไม่เจอ I2C**: ตรวจ VCC/GND/SDA/SCL และลองสลับ 5V กับ 3.3V
- **กดปุ่มแล้วขึ้นผิดตัว**: สลับขาแถว/คอลัมน์ตามตารางด้านบน
- **กดหลักที่ 7 ไม่รับ**: เป็นพฤติกรรมที่ตั้งใจไว้ เพราะจำกัดสูงสุด 6 หลัก
